import { AuthForm } from '@/components/auth-form'
import '../../home.css'

export const metadata = {
  title: 'Sign in | AdPulse Reports',
}

export default function LoginPage() {
  return <AuthForm />
}
